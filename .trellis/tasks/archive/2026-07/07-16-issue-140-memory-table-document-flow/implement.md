# Implementation

1. Read the existing AssistantMemoryPage, AssistantDetailVM, repository methods,
   and route definitions; preserve unrelated #136 edits.
2. Introduce the dialog/event state and document-first list projection.
3. Make template/document persistence awaitable and gate navigation on success.
4. Separate document deletion from template actions.
5. Add/update focused tests and string resources/translations.
6. Run compile and focused tests; record known device-test limitations.
