# 完成 Issues 122-130

## Goal

完成仓库当前公开的 #122–#130，恢复所有已知缺陷并交付约定增强；#129/#130 作为同一缺陷合并实现，同时保留每个 issue 独立、可验证、可关闭的交付证据。

## Requirements

1. 完成 #122 记忆表文档与模板的助手级隔离，保留 GLOBAL 共享语义和旧数据兼容。
2. 完成 #123 YAML 多行 block scalar 的 Skill description 解析，覆盖 UI 与工具注入消费者。
3. 完成 #124 模型搜索中无命中供应商整组隐藏，并同步快捷导航、位置与展开操作。
4. 完成 #125 助手归档/恢复，归档不删除配置、会话或记忆，所有活跃选择入口排除归档助手。
5. 完成 #126 搜索结论写入记忆的助手隔离；Must 范围完整，per-assistant 搜索开关作为非阻塞 Could 项单独评估。
6. 完成 #127 子代理上下文持久化、进程恢复、TTL/LRU 与状态降级。
7. 完成 #128 普通工具并行与同轮多子代理并发的语义解耦，最大并发数真实生效。
8. 完成 #129/#130 工作区图片读取，使视觉内容以模型可消费的图片内容返回；关闭其中一条时注明重复关系。
9. 保留工作区现有 #122 未提交改动和用户 `.npmrc`，不同 issue 的实现不得相互覆盖或误提交本地工具文件。
10. 所有 app 功能变更完成后执行 focused tests、Debug 编译和有设备时 `:app:installDebug`；不默认运行 connected Android tests。

## Acceptance Criteria

- [ ] 八个 open issue 均有与正文逐项对应的代码和测试证据，#129/#130 共用同一修复但分别正确处置。
- [ ] #122–#130（不含重复实现）对应子任务全部通过独立验收与最终跨任务回归。
- [ ] 没有跨助手数据泄漏、归档数据丢失、子代理上下文恢复卡死或工作区图片内容静默丢失。
- [ ] `git diff` 不包含 `.omc/`、密钥、签名文件或无关本地工具改动；Trellis 文件不进入上游交付提交。
- [ ] `CHANGELOG.md` 按仓库格式记录用户可见修复与功能。
- [ ] Gradle 命令全部使用 `--no-daemon`；有在线设备时安装 Debug APK 验收。
- [ ] 代码提交并推送到 `release/rikka-arsucar`，GitHub issues 使用提交/评论证据关闭。

## Notes

- 用户已明确授权创建任务、规划并直接执行完整范围。
- 父任务只负责范围映射和最终集成；实际实现由子任务承担。
