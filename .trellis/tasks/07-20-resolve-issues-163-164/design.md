# Design: Parent integration

Two independent children; no shared runtime contract beyond branch hygiene.

Order: implement #163 first (UI regression), then #164 (feature). Parallel only if agents do not both compile.
