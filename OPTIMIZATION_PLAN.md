# RikkaRs optimization plan

Authoritative constraints and success criteria live in `Prompt.md`. This file is the live candidate ledger; a candidate
is accepted only after evidence, comparable measurement, focused verification, `assembleDebug`, and documentation.

## Measurement baseline

- Start: `release/rikka-arsucar` at `a07d71b3b634d3f97d519d693b58f8174ff083b5`.
- Cold protocol: `.\gradlew --no-daemon clean assembleDebug --profile --no-build-cache
  --no-configuration-cache --quiet`, three serialized samples on the same host.
- Cold wall samples: 259.474 s, 255.280 s, 256.737 s; median 256.737 s.
- Identical APK: `app/build/outputs/apk/debug/app-arm64-v8a-debug.apk`, arm64-v8a debug, 82,140,247 bytes,
  SHA-256 `602F0A34848D96A889E21B993176624F5238EC7F849934D1AA747C89BDA0AB95`.
- JVM tests: all repository `test` tasks passed at baseline.
- detekt/ktlint: N/A; neither task nor configuration existed at baseline. Android Lint is tracked separately.

## Candidate ledger

| ID | Priority | Candidate and evidence | Expected mechanism | Risk | Validation | Status / result |
|----|----------|------------------------|--------------------|------|------------|-----------------|
| B1 | P0 | Enable Gradle parallel execution; ten modules, setting disabled in `gradle.properties` | Overlap independent module compilation/resource/native tasks | Medium: RAM contention or coupled tasks | Same 3-sample cold protocol, tests, assemble | Accepted: median 244.862 s, -4.62%; APK unchanged; install passed |
| B2 | P0 | Verify `web:buildWebUi`; wired to every `preBuild`, but baseline observed UP-TO-DATE | Avoid external pnpm work only if incrementality is broken | Medium: stale embedded UI | Two `:web:preBuild --info` runs and input-change check | Skipped: both runs UP-TO-DATE (22 s / 10 s) |
| B3 | P0 | Audit `api`/`implementation` and unused build dependencies/plugins (first round) | Reduce compile classpaths and artifact transforms | Medium/high compatibility | Dependency insight, consumer search, focused tests, cold median/APK | Superseded by B3-R: first-round skip lacked examination depth |
| B3-R | P0 | Resume full `api`/`implementation` audit; demote non-public `api` deps; move FloatingX/jsoup consumers to direct app deps | Shrink library compile classpaths and stop re-exporting types not in public module APIs | Medium: missing direct dep on consumers after demotion | Static public-surface + import audit (see NOTES); final check: compileDebugKotlin, tests, cold×3 optional | Accepted (check verified): common dropped logging/datetime/commons-text/floatingx; ai dropped logging, added implementation commons-text; search/highlight jsoup/quickjs → implementation; app adds okhttp-logging, floatingx, jsoup. `compileDebugKotlin` PASS; JVM test 960/0/0/3; installDebug on `ebc3de22` PASS. No wall-clock claim until stage-6 cold×3 |
| B4 | P0 | Evaluate `mavenLocal()` resolution cost only if no local artifact is consumed | Avoid local metadata lookup and improve reproducibility | High: local/private dependency breakage | Dependency resolution evidence and clean build | Skipped (reconfirmed): `settings.gradle.kts` still has `mavenLocal()`; catalog uses public Maven/JitPack coords including SNAPSHOT `sqlite-android` which may resolve via mavenLocal in some hosts — no safe removal without resolution log |
| B5 | P0 | kapt → KSP / annotation-processor migration inventory | Avoid dual processors and kapt JVM cost | Low if already KSP; high if processors remain on kapt | Grep kapt/ksp/annotationProcessor across Gradle + sources | Reviewed-skip / N/A: zero `kapt` or `annotationProcessor`; only `ksp(libs.androidx.room.compiler)` in `:app`; root applies KSP plugin `apply false` |
| B6 | P0 | Remove confirmed unused plugins, deps, generated tasks | Reduce configuration and transform work | Medium if false-positive removal | Reference search + dead-code in Gradle only | Partial implement: removed dead `io.objectbox` plugin resolutionStrategy from `settings.gradle.kts` (no ObjectBox usage). Catalog orphans (`dom4j`, `sqlite-vector`, `androidx-navigation2`, `ktor-server-caching-headers`, commented leakcanary) left in place — unused catalog entries do not pull artifacts until referenced |
| R1 | P1 | Cache `MarkdownWeb` HTML template; current function rereads asset per preview | Remove repeated UI-path disk read and invariant template allocation after first use | Low/medium | Focused per-key cache tests, assemble, app install | Accepted: 2 tests pass; assemble/install pass; no Perfetto delta claimed |
| R2 | P1 | Initial Markdown/HTML parsing and Jsoup parsing occur during composition | Move expensive parse off UI thread, reduce first-frame stalls | High: blank/stale content race | Focused state tests plus trace/device long-message check | Reviewed-skip (resume): `Markdown.kt`/`MarkdownNew.kt` still first-compose parse; updates already `flowOn(Default)`; no Macrobenchmark |
| R3 | P1 | Syntax visual transformation uses `runBlocking` for highlighting | Remove main-thread blocking during editing/recomposition | High: offsets/cursor/stale results | Editor behavior tests and device frame trace | Reviewed-skip (resume): `HighlightCodeBlock.kt:519` `runBlocking` inside sync `VisualTransformation.filter`; needs redesign + cursor tests |
| R4 | P1 | LRU key roulette reads/parses/writes cache under global lock per request | Remove repeat read/JSON parse per cache file while retaining synchronous writes and fairness | Medium: crash persistence/concurrency | 3 deterministic tests including 8-thread fairness, assemble, app install | Accepted: `fe91967d`; no wall-time delta claimed |
| R5 | P1 | Conversation/media DAO sort/filter columns have no explicit indexes | Avoid scan/temp sort at large row counts | Medium: migration/write/storage cost | Real-scale `EXPLAIN QUERY PLAN`, migration and DAO tests | Skipped: no production-scale query plan fixture |
| R6 | P1 | Image Base64 encoding and TTS callback reads create large in-memory copies | Reduce peak allocation/callback blocking | Medium/high provider semantics | Focused benchmark/memory trace and provider tests | Reviewed-skip (resume): Export/TTS still allocate full buffers; no memory harness; semantics too broad for safe change |
| R7 | P1 | Compose entity lists missing keys in ChatDrawer; suggestions have no model ID; Coil `ImageRequest` rebuilt every recomposition in `ZoomableAsyncImage` | Stable identity where domain IDs exist + avoid Coil reload churn on parent recomposition | Low: key collisions / request identity | Code review; final check: compileDebugKotlin + install if device available; no frame-time claim | Accepted (check verified): entity-ID keys on ChatDrawer folders/assistants; suggestions retain positional identity because `List<String>` allows duplicates and has no stable ID; remember ImageRequest in ZoomableAsyncImage. compile + install PASS; no frame-time claim |
| R8 | P1 | OkHttp: shared app client vs TTS/MCP private builders | Document connection reuse; only merge if proven duplicate waste | Medium: timeout/interceptor coupling | Static inventory of all OkHttpClient.Builder sites | Reviewed-skip: app single is DI singleton with intentional interceptors; SearchService reuses it; TTS/MCP private clients isolate long timeouts / no logging — merging risks behavior change |
| R5-R | P1 | Resume Room index audit for ConversationEntity / GenMediaEntity sort columns | Optional indexes on assistant_id+update_at / type+create_at | Medium: migration + write cost | Full DAO/entity inventory; still no EXPLAIN fixture | Reviewed-skip with evidence: light projections + Paging already avoid full-row scans on list paths; no production-scale plan → no migration |
| Q1 | P2 | Existing Android Lint baseline includes actionable issues | Reduce known quality/resource debt without adding tools | Low/medium localization/resource risk | One applicable lint run, assemble, APK comparison | Skipped for this run: lint reports 180 errors/61 warnings/1 hint |
| Q1-R | P2 | Reconfirm detekt/ktlint absence for Plan P2 warning target | Honest N/A without adding analyzers | None if N/A | Grep plugins/config/tasks in Gradle/TOML/CI | Reviewed-skip / N/A: zero detekt/ktlint in `*.gradle.kts`, `libs.versions.toml`, `.github/workflows`; Prompt metric remains N/A |
| Q2 | P2 | Search for confirmed dead code and compile-classpath leaks | Reduce code/APK/maintenance only when unreachable | Medium | Full reference search, tests, assemble, APK diff | Superseded by Q2-R / A4: first-round thin skip |
| Q2-R | P2 | Resume dead-code / unused-ref search (Kotlin + resources) | Delete only zero-reference safe items | Medium false-positive | Repo-wide ref search; deprecated serialization kept | Partial: no safe Kotlin type/function delete (`@Deprecated` ToolCall/ToolResult still used in merge/migration); resource dead item handled as A4 |
| Q3 | P2 | Merge duplicate implementations only on clear low-risk win | Reduce maintenance without new abstraction tax | Medium behavior drift | Code-path comparison of Markdown/TTS/OkHttp duplicates | Reviewed-skip: `MarkdownBlock` wraps `MarkdownNew` (facade); TTS multi-client intentional (R8); no merge |
| Q4 | P2 | Module coupling that expands compile/runtime classpath | Keep app-centric graph; no mass restructure | High if wrong demotion | `api(project)` inventory + settings includes | Reviewed-skip after audit: zero `api(project(...))`; all edges `implementation`; residual coupling is intentional app hub + B3-R third-party demotions |
| A1 | P3 | Analyze APK root/dex, assets, native libs, resources | Select size work from actual largest entries | Low for analysis | Exact zip/APK Analyzer breakdown | Completed analysis; no safe size change selected |
| A2 | P3 | `useLegacyPackaging=true` for native libraries | Potential packaging/install-size improvement | High: native/workspace loader behavior | Identical APK diff and device terminal/workspace smoke test | Skipped: native compatibility evidence insufficient |
| A2-R | P3 | Resume packaging / large-asset / shrink review | Document gates; change only with size+compat proof | High packaging/native | Read `app/build.gradle.kts` packaging + assets sizes | Reviewed-skip: keep `useLegacyPackaging=true` + `pickFirsts libtermux`; release already `isShrinkResources=true`; largest assets are jieba dict (~6+5 MB) and banner PNGs (~1.7 MB×3) — functional, not deleted |
| A3 | P3 | Review R8 keep rules/resources/baseline profile | Remove over-keep or improve startup only with release/device proof | High: reflection/startup regressions | Same variant analysis, install and startup verification | Skipped: release-specific proof unavailable |
| A3-R | P3 | Structured R8/ProGuard keep review (resume) | List keep patterns; avoid unproven rule edits | High release-only | Read `app/proguard-rules.pro` + library consumer-rules | Reviewed-skip (rules unchanged): keep kotlinx.serialization, jlatexmath, jackson/auth0; `-dontobfuscate`; JVM-only dontwarns; library consumer-rules empty except document MuPDF; no release APK proof → no keep trim |
| A4 | P3 | Delete confirmed unused drawables after full ref scan | Tiny resource shrink; cleaner lint surface | Low if zero refs | Scan all `res/drawable*` vs `R.drawable`/`@drawable` | Accepted (check verified): deleted `app/src/main/res/drawable/patreon.xml` (0 code/XML refs). compileDebugKotlin + packageDebug/install PASS; exact APK size in stage 6 |
| A5 | P3 | Baseline profile module + generated profiles present? | Startup only if regenerate+install+verify possible | High device/Macrobenchmark cost | Locate plugin, generator, generated prof files | Reviewed-skip (no regen): `:app:baselineprofile` wired; `BaselineProfileGenerator`/`StartupBenchmarks` exist; generated `baseline-prof.txt` + `startup-prof.txt` (~5.0 MB each) already under `app/src/release/generated/baselineProfiles/`; regenerating needs connected device + instrumented run — out of this implement dispatch |
| T1 | P4 | Add regression tests for accepted behavior changes | Prevent optimization regressions | Low | Affected module test tasks and final `test` | Completed via R1/R4 focused tests |
| T1-R | P4 | Tests for **this resume round** behavior changes | Cover only new risk surface | Low | Decide per candidate | Reviewed-skip new tests: B3-R is Gradle-only; R7 is entity-ID key/`remember` identity (no pure unit surface without Compose UI test); A4 resource delete has no JVM unit surface; prior T1/T2 remain valid |
| T2 | P4 | Replace leaking temp-dir setup / real-time test waits when evidence supports it | Delete workspace scanner test directories after each test to prevent disk pollution | Low | Focused workspace test and assemble | Accepted: 11 scenarios pass; cleanup hook added; no production behavior change |
| T3 | P4 | Affected-module test list for final check agent | Single compile owner runs verification | N/A (orchestration) | Document commands only | Executed by check agent: `compileDebugKotlin` PASS; full `test` 960/0/0/3 PASS; `installDebug` on `ebc3de22` PASS; modules: common, ai, search, highlight, app |
| T4 | P4 | Confirm no CI workflow edits; no default AndroidTest | Policy compliance | None | `git status` on `.github` | Confirmed: workflows unchanged this round; no `connectedDebugAndroidTest` scheduled |

## Rejection rules

- Reject build candidates whose comparable three-sample median does not improve beyond normal baseline variance or that
  worsen reliability/memory without compensating evidence.
- Reject runtime/APK candidates without an executable bottleneck measurement or compatibility proof.
- Restore only the current uncommitted candidate after failure. Use a separate exact revert commit for committed work.
- After three same-cause failures, record the candidate in `OPTIMIZATION_NOTES.md` and move on.
